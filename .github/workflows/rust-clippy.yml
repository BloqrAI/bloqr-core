# Rust Build, Test and Security Analysis workflow
# Optimized with better caching, path-based triggers, and consolidated jobs

name: Rust CI

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: ["main"]
    paths:
      - 'src/rules-compiler-rust/**'
      - 'src/rules-validator/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/rust-clippy.yml'
  pull_request:
    branches: ["main"]
    paths:
      - 'src/rules-compiler-rust/**'
      - 'src/rules-validator/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/rust-clippy.yml'
  schedule:
    - cron: '24 0 * * 6'  # Weekly security scan on Saturday

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  RUST_BACKTRACE: short

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          components: rustfmt, clippy

      - name: Cache Cargo
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Check formatting
        run: |
          cargo fmt -p rules-compiler -p rules-validator-core -p rules-validator-cli -- --check

      - name: Build workspace
        run: cargo build --workspace --all-features

      - name: Run tests
        run: cargo test --workspace --all-features

      - name: Run clippy
        run: |
          cargo clippy --no-deps -p rules-compiler -p rules-validator-core -p rules-validator-cli \
            --all-features -- -W clippy::all -W clippy::correctness -W clippy::suspicious

  security-analysis:
    name: Security Analysis
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule' || github.event_name == 'push'
    permissions:
      contents: read
      security-events: write
      actions: read

    steps:
      - uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          components: clippy

      - name: Cache Cargo
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-security-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-security-

      - name: Install SARIF tools
        run: cargo install clippy-sarif sarif-fmt

      - name: Run clippy for security analysis
        run: |
          cargo clippy --no-deps -p rules-compiler -p rules-validator-core -p rules-validator-cli \
            --all-features --message-format=json | clippy-sarif | tee rust-clippy-results.sarif | sarif-fmt
        continue-on-error: true

      - name: Upload SARIF results
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: rust-clippy-results.sarif
          wait-for-processing: true
