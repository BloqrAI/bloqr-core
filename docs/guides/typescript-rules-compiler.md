# TypeScript Rules Compiler Guide (moved)

This guide has been superseded by and merged into
[`compiler-core-guide.md`](compiler-core-guide.md), the canonical guide for
`@bloqr/compiler-core` (`src/compilers/typescript/`). This page previously
duplicated that content under the project's old pre-#331/#372 "Rules
Compiler" branding; it is kept only as a redirect so existing links and
bookmarks still resolve.

See [`@bloqr/compiler-core` Guide](compiler-core-guide.md) for installation,
CLI usage, library API (`BloqrCompiler`), configuration, and CI/CD examples.
