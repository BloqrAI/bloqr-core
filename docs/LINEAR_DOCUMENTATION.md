# Linear Documentation

The Linear import tool (which parsed this file and imported it into Linear as a project + issues) moved to [`BloqrAI/bloqr-apiclients`](https://github.com/BloqrAI/bloqr-apiclients) along with the other API clients.

This file previously held a repository-overview document formatted for that tool to import. That content described the pre-split repository layout (`data/`, the AdGuard DNS API clients, and the Linear tool all living here) and has since been superseded by:

- The root [`README.md`](../README.md) for a public-facing overview
- [`CLAUDE.md`](../CLAUDE.md) for agent-facing instructions kept in sync with the current structure
- [`docs/README.md`](README.md) for the full documentation index

If you need to re-import repository documentation into Linear, see the import tool's own README in `BloqrAI/bloqr-apiclients` for current usage instructions.
